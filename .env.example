# .env.example
PORT=4000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=coffee_shop_finder

JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Optional: only needed if syncing cafes from Google Places
GOOGLE_MAPS_API_KEY=